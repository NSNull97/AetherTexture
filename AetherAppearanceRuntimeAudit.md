# AetherAppearance Runtime Audit

This began as the Phase 0 audit for replacing init-time bar themes with
app-level `AetherAppearance`. The current migration extends that runtime to
three appearance styles and reusable surface consumers. Rows describing the
old ownership model are retained as architectural history; migration actions
describe the current target state.

| Current type | Current theme source | Init has theme? | Runtime theme updates? | Bar-level override? | Should keep? | Migration action | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `AetherNavigationController` | Owns `NavigationControllerTheme` and derives `NavigationBarPresentationData` from it. | Yes: `init(mode:theme:)` and `init(rootViewController:mode:theme:)`. | Yes: `updateTheme(_:)` replaces the controller theme and refreshes the shared bar/split containers. | Indirectly through `AetherViewController.setNavigationBarPresentationData`. | Keep controller, remove theme as primary source. | Add no-theme primary init; resolve from `AetherAppearance`; keep deprecated theme init/update as legacy adapters that become bar overrides. | Current theme controls status bar, empty area, default nav bar colors, glass edge effect radii, and split container propagation. |
| `NavigationControllerTheme` | Explicit app/user-created value passed into navigation controller. | Yes, required by existing primary nav init. | Mutable only by replacing full theme through `updateTheme(_:)`. | Not partial; it is a full controller theme. | Keep as deprecated compatibility type. | Add factory from `AetherAppearance`; mark old init path as compatibility; stop treating it as app appearance source of truth. | Required for source compatibility with existing demos/tests and apps. |
| `NavigationBarTheme` | Stored inside `NavigationBarPresentationData`; used directly by `NavigationBarImpl`. | Yes through `NavigationBarPresentationData(theme:)`. | Yes when `NavigationBarImpl.updatePresentationData` receives a new presentation data object. | Yes, screens can set explicit `NavigationBarPresentationData`. | Keep as low-level compatibility/presentation adapter. | Add resolved appearance pipeline and conversion from resolved nav appearance to legacy `NavigationBarTheme` for current renderer. | Already contains `edgeEffectAlpha`, `edgeEffectBlurRadiusAtEdge`, `edgeEffectBlurRadiusAtFade`, and `edgeEffectStyle`. |
| `NavigationBarImpl` | Reads `presentationData.theme` directly for background, separator, button colors, glass mode, edge effect. | Yes via `init(presentationData:)`. | Yes via `updatePresentationData`. | Uses whatever `NavigationBarPresentationData` caller supplies. | Keep renderer. | Feed it presentation data derived from `AetherNavigationBarResolvedAppearance`; add invalidation entry points through controllers. | This avoids recreating bars/windows while replacing the upstream source. |
| `NavigationSplitContainer` | Receives `NavigationControllerTheme` from `AetherNavigationController`. | Yes, through container init. | Yes via `container.updateTheme(theme:)`. | No independent partial override. | Keep container. | Continue receiving derived compatibility theme from resolved app appearance until renderer is fully native-resolved. | Split mode must not keep app-owned theme as its own source of truth. |
| `AetherTabBarController` | Owns `TabBarView.Theme` in `tabBarTheme`. | Yes: `init(tabBarTheme:)`. | Yes: `tabBarTheme.didSet` calls `tabBarView.updateTheme`. | Full tab-bar theme only. | Keep controller, remove theme as primary source. | Add no-theme primary init; resolve from `AetherAppearance`; keep deprecated `tabBarTheme` and `init(tabBarTheme:)` as compatibility shims. | Tab controller never owns nav bars; each tab brings its own navigation controller. |
| `TabBarView.Theme` | Local theme struct used by `TabBarView`. | Yes: `TabBarView(theme:)`. | Yes: `updateTheme(_:)`. | Full theme only. | Keep as renderer adapter. | Add conversion from `AetherTabBarResolvedAppearance` to `TabBarView.Theme`. | Current theme controls liquid-glass style, separator color, edge effect alpha/radius, sizing and colors. |
| `TabBarView` | Reads `Theme` directly and owns edge-effect view and glass/lens views. | Yes. | Yes via `updateTheme`. | No partial override API. | Keep renderer. | Feed it a theme derived from resolved tab appearance; expose update path from app appearance invalidation. | Current edge-effect implementation is reusable; no new private API should be introduced here. |
| `AetherSearchController` bottom search | Pulls edge-effect parameters from host navigation bar theme, with hardcoded fallback values. | No app-level theme init; search configuration owns placement/text only. | Edge-effect refresh happens during layout; `updateEdgeEffect(color:)` only updates color. | No structured override. | Keep component. | Add `AetherSearchResolvedAppearance`; resolve bottom search from app appearance instead of reaching into nav theme when no explicit nav bar override exists. | Bottom search currently exists as a pill plus `EdgeEffectView`, not as a separate app-surface model. |
| `AetherViewController.inputBarAccessoryView` | No appearance model; caller supplies arbitrary view. | No. | Layout invalidation only. | `inputBarAccessoryUsesBottomEdgeEffect` boolean exists. | Keep host mechanism. | Add `AetherInputBarResolvedAppearance` as contract for future/dedicated bars; current integration exposes resolved input appearance but does not invent a new input bar renderer. | There is no dedicated `AetherInputBar` type in the codebase. |
| `EdgeEffectView` | Receives concrete tint/alpha/radii per call. | No. | Yes via `update(...)`. | Caller-controlled values. | Keep primitive. | Add `AetherEdgeEffectAppearance` and feed values from resolved surface appearances. | Existing variable blur path uses private CoreAnimation filters; new runtime must not add more private/reflection dependencies. |
| `EdgeEffectAttachment` | Standalone attachment configuration. | No app theme init. | Yes by calling `setEdgeEffect` again or relayout. | Full attachment config. | Keep out of app runtime. | Optionally add helpers from `AetherEdgeEffectAppearance`; no required migration for app bars. | General-purpose view extension, not a navigation/tab runtime owner. |
| `SystemGlassEffectStyle` | Existing enum with `.regular` and `.clear`; used by navigation themes and glass helpers. | No. | N/A. | N/A. | Keep, extend carefully. | Add `.strong` because `AetherAppearance.liquidGlassV2` requires it; preserve `.clear` for existing APIs. | The spec allows creating `.regular`/`.strong` if absent. It is present, so add only the missing required style without removing compatibility `.clear`. |
| `SystemGlassEffect` | Uses public `UIGlassEffect` on iOS 26 when available and material blur fallback elsewhere. | No. | N/A. | Style argument. | Keep helper. | Map `.strong` to the densest public glass/material available. | Do not add new runtime calls to private glass classes. |
| `VisualEffectView` / `VariableBlurView` / backdrop helpers | Internal glass/blur infrastructure. | No app theme init. | Internal update APIs. | N/A. | Keep existing implementation but do not expand. | Document in availability audit; new appearance runtime should use public model/resolvers and existing primitives only. | Existing code contains private/reflection paths; the new runtime should not make them part of public appearance API. |
| `AlertTheme`, `ActionSheetTheme`, `ToastTheme`, `TooltipTheme`, `SkeletonTheme`, `SliderTheme`, `ToolbarTheme`, `SegmentedControlTheme` | Component-local themes. | Yes for some components. | Component-specific. | Component-specific. | Keep as customization APIs. | Resolve their surface renderer from the app appearance while preserving component-local color/content overrides. | Legacy must not leave these surfaces on a glass backend; local themes remain adapters, not a second style system. |

## Required Migration Shape

- `AetherAppearanceStyle` is fixed to `.legacy`, `.liquidGlassV1`, and `.liquidGlassV2`; no automatic style, custom style IDs, or OS-based selection is added.
- `AetherApplication` owns a single app-level style through `AppearanceStyle(...)` and `.appearanceStyle(...)`.
- `AetherAppEnvironmentValues` carries both style and resolved `AetherAppearance` for app and scene render contexts.
- Visible bars and registered reusable surface hosts update from the current appearance by replacing renderer material in place, not by recreating windows, root controllers, or component content.
- Local screen-specific customization moves to partial `AetherAppearanceOverride` values and surface resolvers, not to app-wide theme replacement.
- Deprecated theme APIs remain compatibility adapters to prevent breaking existing call sites during migration. They are distinct from the new `.legacy` appearance.
- Deprecated `.iOS26` and `.iOS27` aliases map to the two Liquid Glass generations without becoming extra enum cases.
- Codable output uses `legacy`, `liquid-glass-v1`, and `liquid-glass-v2`; decoding accepts historical iOS 26/27 spellings.
